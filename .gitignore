.claude/

node_modules/
dist/
.env
.env.local
.env.*.local

frontend/node_modules/
frontend/dist/
coverage/

# The IPC source corpus. Kept out of the repository deliberately; the committed
# artefact of record is backend/src/data/ipcKnowledge.json, produced from these
# by `npm run ingest:ipc`. See backend/README.md → IPC knowledge base.
docs/markdown/

backend/node_modules/
backend/.env
backend/storage/

credentials.json
token.json
*-credentials.json
*-token.json
*service-account*.json
*.pem
*.p12

*.log
.DS_Store

# Lockfiles are deliberately NOT ignored. Both are tracked, and a reproducible
# `npm ci` in CI and on a deployment host depends on them. Rules ignoring them
# used to sit here; they were inert against files already tracked, but they
# said the opposite of the intent.
