# Internal authenticated tool (IPC Query Management System).
# Deliberately excluded from search indexing; see docs — the login page is the
# only public surface and there is nothing useful to crawl behind it.
User-agent: *
Disallow: /
